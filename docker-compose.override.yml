version: "3.8"

# Development overrides — exposes service ports for direct debugging.
# In production, only the gateway port (8080) should be exposed externally.

services:
  orders-api:
    ports:
      - "5001:80"

  inventory-api:
    ports:
      - "5002:80"
